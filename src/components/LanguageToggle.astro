---
import { blogPostUrl } from "../lib/blog";

interface Props {
  currentLang: "en" | "fr";
  postSlug: string;
  availableLangs: ("en" | "fr")[];
}

const { currentLang, postSlug, availableLangs } = Astro.props;
---

{
  availableLangs.length > 1 && (
    <div
      class="border-sage-6 flex gap-1 rounded-lg border p-0.5 text-sm"
      role="group"
      aria-label="Language"
    >
      {availableLangs.map((lang) => (
        <a
          href={blogPostUrl(postSlug, lang)}
          class:list={[
            "rounded-md px-3 py-1 font-medium uppercase transition-colors",
            lang === currentLang
              ? "bg-teal-9 text-white"
              : "text-sage-11 hover:bg-sage-3",
          ]}
          aria-current={lang === currentLang ? "page" : undefined}
        >
          {lang}
        </a>
      ))}
    </div>
  )
}
